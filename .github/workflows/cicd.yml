name: Deploy to pages

on:
  push:
    branches: [ "main" ]
  # pull_request:
  #  branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Install dependencies
      run: yarn install

    - name: Build the project
      run: yarn build

    - name: Print working directory
      run: pwd

    - name: List contents of dist directory
      run: |
        echo "Listing contents of /home/runner/work/routerra-landing:"
        ls -al /home/runner/work/routerra-landing
        echo "Listing contents of /home/runner/work/routerra-landing/routerra-landing:"
        ls -al /home/runner/work/routerra-landing/routerra-landing
        echo "Listing contents of /home/runner/work/routerra-landing/routerra-landing/dist:"
        ls -al /home/runner/work/routerra-landing/routerra-landing/dist

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./dist
